# Main configs
SCENE_FILE_NAME=scene
FPS_CAP=60
FULLSCREEN=true
WINDOWED_WIDTH=1280
WINDOWED_HEIGHT=720

# Outputs
RECORD_VIDEO=false
RECORD_SCENE=false
LOG=false

# Drawing
OBJECT_DEFINITION=8
SHOW_FPS=true

# Algorithms (BROAD_PHASE options are NONE, AABB, OBB, SAP or SAPOBB)
BROAD_PHASE=SAP

# Run mode (options are DEFAULT, TEST, BENCHMARK, REPLAY)
RUN_MODE=DEFAULT
NUM_RUNS_FOR_BENCHMARK=5
SCENE_REPLAY_FOLDER_NAME=scene_9-10-2021-18_4

# Frame to stop simulation, use -1 to run indefinitely
STOP_AT_FRAME=-1