SCENE_FILE_NAME=scene
FPS=60
WINDOW_WIDTH=1280
WINDOW_HEIGHT=720

# Object drawing
NUM_LAT_LONGS=8
DRAW_HALF_WHITE=false

# Algorithms (BROAD_PHASE options are NONE, BF, SAP or MTSAP)
BROAD_PHASE=SAP
USE_MID_PHASE=false

# Logging
LOG=true
LOG_OUTPUT_FILE_NAME=output.csv

# Run mode (options are NORMAL, TEST, BENCHMARK, RECORD_VIDEO)
RUN_MODE=NORMAL
NUM_RUNS=1
NUM_FRAMES_PER_RUN=300