TODO LIST

- Remove object pointers from Collision, use map's key to index all objects array instead.
- Fix vector magnitude in all NarrowPhaseCollisionDetection functions.
- Fix bug in Planes.
- Model (or fix) resting contact.

Nice to have:
- Fix penetration between objects (may be related to resting contact).