# Main configs
SCENE_FILE_NAME=matrix-many
FPS_CAP=60
FULLSCREEN=false
WINDOWED_WIDTH=1280
WINDOWED_HEIGHT=720

# Outputs (for DEFAULT mode only)
LOG=true
RECORD_SCENE=true
RECORD_VIDEO=true # FPS_CAP must be 25, 30, 60 or 120

# Drawing
OBJECT_DEFINITION=8
SHOW_FPS=true

# Algorithms (BROAD_PHASE options are NONE, AABB, OBB, SAP or SAPOBB)
BROAD_PHASE=SAP

# Run mode (options are DEFAULT, TEST, BENCHMARK, REPLAY)
RUN_MODE=DEFAULT
NUM_RUNS_FOR_BENCHMARK=5
SCENE_REPLAY_FOLDER_NAME=test-6336--2021-10-19--17-17-02

# Frame to stop simulation, use -1 to run indefinitely
STOP_AT_FRAME=900